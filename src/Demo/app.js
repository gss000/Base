import React from 'react';
import ReactDOM from 'react-dom';
import { Route, Switch, HashRouter, BrowserRouter, } from 'react-router-dom';

ReactDOM.render(
  <HashRouter></HashRouter>, document.getElementById('app')
);